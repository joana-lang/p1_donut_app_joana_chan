import 'package:flutter/material.dart';

class panCakeTab extends StatelessWidget {
  const panCakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("panCakeTab"));
  }
}
